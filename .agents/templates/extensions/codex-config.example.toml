# code-agent-template:managed
# Inactive Codex extension examples. Review and copy only the required entries
# into `.codex/config.toml`; this file is never loaded from its current path.

# [mcp_servers.reviewed-server-name]
# command = "replace-with-reviewed-command"
# args = []
# env_vars = ["SERVICE_API_TOKEN"]

# Add hooks only after checking the current Codex hook schema and reviewing the
# repository-relative command, matcher, timeout, failure behavior, and sandbox.
