---
description: Delegate independent work to constrained researcher, reviewer, or test-runner roles
---

<!-- code-agent-template:managed -->
# Delegate Work

Load and follow the `delegate-work` skill from `.agents/skills/delegate-work/SKILL.md`.

Use the minimum permissions, keep architectural decisions with the primary agent, and avoid overlapping writes.
