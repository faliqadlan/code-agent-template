---
description: Safely stage or explicitly activate plugins, MCP servers, and hooks
---

<!-- code-agent-template:managed -->
# Configure Extensions

Load and follow the `configure-extensions` skill from `.agents/skills/configure-extensions/SKILL.md`.

Stage configuration by default. Live activation, dependency installation, and permission changes require explicit authorization.
