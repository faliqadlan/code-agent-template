---
description: Inspect this repository and create or refresh verified project context
---

<!-- code-agent-template:managed -->
# Onboard Repository

Load and follow the `onboard-repository` skill from `.agents/skills/onboard-repository/SKILL.md`.

Write only `.agents/context/project.md`. Cite repository evidence, record unknowns, and do not install dependencies or initialize Git.
