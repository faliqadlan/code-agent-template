# Synthetic Repository

This fixture contains no secrets, external integrations, or consequential data.

## Verification

Run `python -m unittest discover -s tests` only when the evaluation case explicitly permits it.
